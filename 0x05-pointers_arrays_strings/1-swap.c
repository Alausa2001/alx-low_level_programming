#include "main.h"
/**
 * swap_int - sswap the value of
 * two integers
 * @a: input integer (pointer)
 * @b: integer pointer
 * Return: void
 */
void swap_int(int *a, int *b)
{
	int p;
	int q;

	a = &p;
	b = &q;
	*a = q;
	*b = p;
}
