#include "main.h"
/**
 * _isupper - checks for upper case letters
 *
 * @c: character being checked
 * Return: either 0 or 1
 */
int _isupper(int c)
{
	if (c >= '65' && c < '91')
		return (1);
	return (0
}
