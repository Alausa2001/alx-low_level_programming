#include "main.h"
/**
 * mul - multiply two integers
 *
 * @a: input integer
 * @b: input integer
 * Return: always 0
 */
int mul(int a, int b)
{
	a = c;
	b = d;

	_putchar(c * d);
	result = c * d;
	return (result);
}
